# tests/

Reserved for automated tests.

**No tests exist yet.** Testing conventions will be added alongside implementation (see `DEVELOPMENT.md`). Planned: Python `pytest` unit/integration tests, frontend tests, and agent/ML evaluation as the relevant modules are built.