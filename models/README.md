# models/

Reserved for trained model artifacts (weights, checkpoints, evaluation results) and model documentation.

Model files are git-ignored (see `.gitignore`). Record how to reproduce training rather than committing large artifacts.

Not implemented yet. Planned use begins in Q2 (ML/CV agents and change detection).